package com.jlangen.vaultbox.models

import android.support.annotation.DrawableRes
import com.jlangen.vaultbox.R

enum class VaultIcon(val id: Int, @DrawableRes val iconId: Int) {
    KEY(0, R.drawable.ic_key)
}